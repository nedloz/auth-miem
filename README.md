Как это использовать:

Установи библиотеку: pip install requests

В первом окне терминала запусти свой FastAPI сервер: uvicorn app.main:app --reload --port 8000

Во втором окне терминала запусти этот скрипт: python test_flow.py